
[] display all items in the pomodoro object for testing purposes
	[] loop over all items in the object continuously with 2-3 second delay (1 second is definately too short)
	[] hitting a button in browser halts loop

[] use setInterval function to display a count down timer for the appropriate time
	[] if in "focus" state, count down from 25 minutes
	[] if in "rest" state, count down from 5 minutes
	[] after rest state, state becomes "waiting" (I believe) and a button must be pushed to start next timer
